using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Nito.AsyncEx;
using PromoStudio.Data;

namespace PromoStudio.RenderQueue.Tests
{
    [TestClass]
    public class UnitTest1
    {
        //[TestMethod]
        //public void TestMethod1()
        //{
        //    // Arrange
        //    var ds = new DataService(new DataWrapper(new ConnectionManager()));
        //    var renderQueue = new QueueProcessor(ds, new AmazonS3StorageProvider);

        //    AsyncContext.Run(() =>
        //    {
        //        //Act
        //        renderQueue.Execute();
        //    });
        //}
    }
}
